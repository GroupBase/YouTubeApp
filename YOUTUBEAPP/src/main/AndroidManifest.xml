<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.thn.groupbase.gameshowtv">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="dev.vn.groupbase.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version"/>
        <activity android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="dev.vn.groupbase.activity.PlayListActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name="dev.vn.groupbase.activity.PlayListItemsActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait" />
        <activity
            android:name="dev.vn.groupbase.activity.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait" />
        <activity
            android:name="dev.vn.groupbase.activity.VideoPlayActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"  />
        <!--start app-->
        <!--<activity android:name="com.startapp.android.publish.list3d.List3DActivity"-->
            <!--android:theme="@android:style/Theme" />-->

        <!--<activity android:name="com.startapp.android.publish.OverlayActivity"-->
            <!--android:theme="@android:style/Theme.Translucent"-->
            <!--android:configChanges="orientation|keyboardHidden|screenSize" />-->

        <!--<activity android:name="com.startapp.android.publish.FullScreenActivity"-->
            <!--android:theme="@android:style/Theme"-->
            <!--android:configChanges="orientation|keyboardHidden|screenSize" />-->
        <!--start app-->
        <!--amobi-->
        <activity android:name="vn.amobi.util.ads.video.player.VideoActivity" > </activity>
        <meta-data android:name="vn.amobi.util.ads.widget_id" android:value="@string/amobi_id" >
        </meta-data>
        <!--amobi-->
        <!--Include the AdActivity configChanges and theme. -->
        <activity android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />
    </application>

</manifest>